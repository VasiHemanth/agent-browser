node_modules/
*.log
.DS_Store

# Adapter selection is per-machine; the committed config.json is the default.
server/config.local.json

# API keys live in ~/.agentchat/keys.json, never in the repo. Belt and braces:
**/keys.json
.env
.env.*
